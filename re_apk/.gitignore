# Heavy / derived RE artifacts — keep out of git.
# Original app bundle (186MB+), decompiled tree (~520MB), tooling, logs, extracted assets.
com.govee.home_*/
decompiled/
tools/
logs/
assets/

# Keep (do NOT ignore): the authored spec + the extraction workflow
!GOVEE_BLE_PROTOCOL.md
!spec_sections/
!extract_ble_spec.workflow.js
!README.md
